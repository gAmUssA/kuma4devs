apiVersion: v1
kind: Service
metadata:
  name: meeting
  labels:
    app: meeting
spec:
  ports:
    - port: 8080
      protocol: TCP
  clusterIP: None
  selector:
    app: meeting
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: meeting
  labels:
    app: meeting
spec:
  replicas: 1
  selector:
    matchLabels:
      app: meeting
  template:
    metadata:
      labels:
        app: meeting
    spec:
      containers:
        - name: meeting
          image: gamussa/mesh4java-meet
